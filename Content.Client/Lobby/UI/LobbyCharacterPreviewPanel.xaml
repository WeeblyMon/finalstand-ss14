<Control
    xmlns="https://spacestation14.io"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:Content.Client.UserInterface.Controls"
    xmlns:profile="clr-namespace:Content.Client.Lobby.UI.ProfileEditorControls">
    <BoxContainer Name="VBox" Orientation="Vertical">
        <BoxContainer Name="Loaded" Orientation="Vertical"
                      Visible="False">
            <Label Name="Summary" HorizontalAlignment="Center" Margin="3 3" StyleClasses="FSTextDim"/>
            <BoxContainer Name="ViewBox" Orientation="Horizontal" HorizontalAlignment="Center">
                <profile:ProfilePreviewSpriteView Name="ProfilePreviewSpriteView"
                                                  OverrideDirection="South"
                                                  Scale="4 4"
                                                  MaxSize="112 112"
                                                  Stretch="Fill"
                                                  Access="Public" />
            </BoxContainer>
            <Label Name="PerkPointsLabel" HorizontalAlignment="Center" Margin="3 6 3 1" StyleClasses="FSTextGold"/>
            <!-- FINALSTAND: level / XP / prestige -->
            <Label Name="LevelLabel" HorizontalAlignment="Center" Margin="1 1" StyleClasses="FSTextGold"/>
            <Label Name="XpLabel" HorizontalAlignment="Center" Margin="1 1" StyleClasses="FSTextDim"/>
            <Button Name="AugmentShopButton" Text="PERKS"
                    HorizontalAlignment="Center"
                    Margin="0 4 0 0"/>
            <Button Name="CharacterSetup" Text="{Loc 'lobby-character-preview-panel-character-setup-button'}"
                    HorizontalAlignment="Center"
                    Margin="0 4 0 0"/>
            <Button Name="PrestigeButton" Text="✦ PRESTIGE"
                    HorizontalAlignment="Center"
                    Margin="0 4 0 0"
                    Visible="False"/>
        </BoxContainer>
        <Label Name="Unloaded" Text="{Loc 'lobby-character-preview-panel-unloaded-preferences-label'}" StyleClasses="FSTextDim"/>
    </BoxContainer>
</Control>
